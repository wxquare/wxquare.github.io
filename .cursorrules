# Cursor 适配入口

本文件只负责把 Cursor 接入仓库规范，不维护独立规则。唯一规范源是 [`AGENTS.md`](./AGENTS.md)。

## 加载顺序

1. 处理任何任务前先读取 `AGENTS.md`。
2. 文章创建、整理或审查时重点读取第 3、4、7、10、11 节。
3. AI 目录任务额外读取第 10.4 节；分类目录映射按 `.agents/config/post-categories.json` 供工具使用。
4. 规则与本文件冲突时，以 `AGENTS.md` 为准，并将需要长期保留的规范补回 `AGENTS.md`。

## Cursor 操作路由

- 新建文章：按 `AGENTS.md` 第 7.1 节执行；需要模板时使用 `.agents/skills/new-post/SKILL.md` 的执行步骤。
- 审查文章：按 `AGENTS.md` 第 7.2 节执行，并参考 `.agents/skills/review-post/SKILL.md` 的检查流程。
- 整理、统计、检查链接：分别使用 `.agents/skills/organize-posts/`、`.agents/commands/stats.md`、`.agents/skills/link-check/` 的执行步骤。
- 构建和发布：遵循 `AGENTS.md` 第 11 节及 `tools/pre-commit-check.sh`。

Cursor 专属内容仅限加载方式、快捷操作和界面交互；分类、标签、图片、命名、教程及质量要求不得在此重复定义。
