# 第 2 章 技术方案设计方法论

> 本章基于 `source/_posts/system-design/06-tech-design-methodology.md` 重组而成。原文中分散的模板、模式、工具和速查内容被压缩为一套可执行的方法论，优先回答“如何做设计”。

## 需求澄清与问题定义

很多技术方案质量不高，不是因为实现能力不足，而是因为问题定义模糊。一个成熟的设计文档，第一步不是开始画图，而是明确系统到底要解决什么问题。

### 为什么要先写清问题

技术方案至少承担四个作用：

- 对齐理解：让业务、研发、测试、运维对目标和边界达成一致。
- 量化工作：把看似一句话的需求，拆成可估算、可排期的工程工作。
- 固化术语：避免团队对关键概念、模块职责和数据对象理解不一致。
- 沉淀知识：让后来者能快速理解系统，而不是只能从代码和口口相传里猜设计意图。

### 需求澄清的最小清单

在进入方案设计前，至少要回答以下问题：

- 这个系统服务谁，核心使用场景是什么。
- 当前方案或旧系统的痛点是什么。
- 哪些指标是必须达成的，例如延迟、吞吐、稳定性、正确性。
- 哪些内容属于非目标，本期明确不做。
- 外部依赖有哪些，是否受别的团队、第三方服务或基础设施约束。

一个常见错误是把“背景”写成“解决方案摘要”。背景部分应该聚焦现状、问题和目标，而不是提前给出实现细节。否则评审者很难区分哪些是事实，哪些是方案假设。

### 明确目标与非目标

设计不是追求完美，而是追求平衡。把目标和非目标显式写出来，有两个好处：

- 帮助评审者理解你的取舍依据。
- 防止项目在推进过程中不断被隐性扩 scope。

例如，一个面向内部运营的报表系统，本期目标可能是“支持 T+1 数据更新、百人级并发查询、操作留痕”，非目标则可能包括“实时分析”“跨地域多活”“统一对外开放 API”。这些边界会直接影响存储、链路和投入方式。

## 容量估算与约束识别

好的方案不是从组件清单开始，而是从数字开始。数字不需要百分之百精确，但必须足够支撑决策。

### 为什么容量估算是设计入口

容量估算回答的是两个问题：

- 方案大概要承受多大规模。
- 哪些约束会成为架构边界。

缺少数量级，很多讨论都会变成空话。比如“是否需要分库分表”“要不要上消息队列”“缓存层值不值得做”，都依赖数据规模与流量模型。

### 估算时至少关注什么

常见估算维度包括：

- 请求量：平均 QPS、峰值 QPS、突发流量倍数。
- 数据量：单条大小、日增量、冷热分布、保留周期。
- 读写模型：读多写少、写多读少、是否存在热点。
- 时延目标：接口 RT、异步任务完成时间、批处理窗口。
- 可用性目标：SLA、可接受失败率、恢复时间目标。
- 外部约束：预算、人力、上线时间、合规、安全审计。

原始源稿中的延迟、带宽、存储速查图，本质上都在提醒一件事：远程调用、磁盘 IO、网络传输和内存访问根本不是一个数量级。设计者需要对这些数量级有直觉，才能在“同步还是异步”“本地缓存还是远程查询”“批量还是逐条”的问题上做出合理选择。

### 识别真实约束，而不是理想约束

工程里最重要的约束往往不是技术本身，而是现实条件：

- 项目只有两周交付，意味着不能引入过多新基础设施。
- 团队没人维护分布式存储，意味着方案要优先复用成熟组件。
- 依赖下游 SLA 很弱，意味着要提前设计降级、重试和隔离。
- 数据涉及权限和审计，意味着安全、追踪和权限模型必须前置。

容量估算的意义，不只是为了“算多大”，更是为了知道系统最先会被什么限制住。

## 架构拆解与核心链路

当问题和约束清楚之后，才进入架构拆解。拆解的目标不是把图画复杂，而是把职责边界、调用方向和关键路径讲明白。

### 先画高层，再落细节

一个合格的高层设计，通常应该先回答：

- 系统由哪些核心模块组成。
- 请求从入口到结果返回，会经过哪些关键环节。
- 哪些模块在主链路上，哪些是异步旁路。
- 哪些依赖来自外部系统，失败时如何影响主流程。

这一步可以结合业务架构、应用架构、技术架构和数据架构去描述，但重点不是“画了几张图”，而是让评审者理解模块为什么这么拆、这些拆分如何支撑目标。

### 架构拆解的常见方式

源稿给了很多架构模式和基础设施素材，迁移到书里可以归纳成几类通用拆法：

- 按业务能力拆：订单、库存、支付、搜索等独立演进。
- 按链路层次拆：接入层、网关层、应用服务层、数据层。
- 按同步异步拆：核心事务链路保持短而稳，耗时任务转异步。
- 按变化频率拆：高频变动模块与稳定底座分离，降低发布耦合。

### 核心链路比全景图更重要

很多设计评审失败，不是因为没有覆盖面，而是没有突出核心链路。所谓核心链路，就是最能决定系统价值和风险的那条主路径。例如：

- 下单系统中的“校验库存 -> 扣减库存 -> 生成订单 -> 支付确认”
- 内容平台中的“写入内容 -> 审核 -> 建索引 -> 用户检索”
- 网关系统中的“鉴权 -> 路由 -> 限流 -> 转发 -> 记录日志”

如果核心链路没有讲清楚，后面的高可用、扩展性和风险管理就很难讨论扎实。

## 数据流、状态流与边界划分

系统设计不只是模块设计，更是数据与状态的设计。模块边界一旦模糊，最终往往会在一致性、权限和故障恢复上出问题。

### 数据流回答“信息怎么走”

设计数据流时，至少要说明：

- 数据从哪里来，由谁写入。
- 哪些服务读主库，哪些走缓存、副本或索引。
- 哪些数据需要同步返回，哪些允许异步收敛。
- 数据在不同存储之间如何复制、投递、校验和修复。

这一步直接影响数据库选型、缓存策略、消息模型和搜索索引构建方式。比如读多写少的场景偏向缓存和副本扩展；写入顺序敏感的场景则更强调串行化、事务和幂等。

### 状态流回答“系统怎么演进”

很多业务的复杂度不在 CRUD，而在状态机。订单、审批、履约、任务调度都属于典型状态驱动场景。设计时要明确：

- 系统有哪些核心状态。
- 状态转移由谁触发，是否允许回滚或补偿。
- 并发更新时如何防止状态错乱。
- 失败重试会不会造成重复执行。

这也是接口幂等性设计重要的原因。对于创建类接口、支付回调、任务触发等高风险操作，幂等键、业务去重表、状态机约束通常是必须的。

### 边界划分回答“谁负责什么”

边界划分的目标是高内聚、低耦合、隔离变化。可以借助这些原则检查设计：

- 一个模块的职责是否足够单一。
- 一个接口是否暴露了过多实现细节。
- 一个服务是否承担了本不属于它的状态管理责任。
- 未来变化最频繁的部分，是否被隔离到了独立边界内。

源稿中的 SOLID、正交性、高内聚、低耦合，本质上都在强调同一件事：系统应该把容易变化的部分隔离开，让稳定部分尽量少受影响。

## 高可用、高性能与扩展性设计

功能正确只是起点。系统一旦上线，真正考验设计质量的，是高峰流量、依赖异常和持续演进。

### 高可用设计

高可用不是“多部署几台机器”，而是围绕失败场景建立韧性。常见手段包括：

- 冗余：多实例、多副本、跨可用区部署。
- 保护：超时、重试、限流、熔断、隔离。
- 降级：非核心功能在高压下主动关闭或返回兜底结果。
- 恢复：备份、故障切换、补偿任务、数据修复机制。

设计时还需要把外部依赖纳入考虑。某个下游服务 SLA 很差时，你不能只写“依赖它返回结果”，而要说明缓存、默认值、异步补偿或人工兜底策略。

### 高性能设计

高性能通常依赖三类方法：

- 减少单次请求成本：缓存、批量、连接复用、索引优化。
- 缩短关键路径：把非必要工作从同步主链路移到异步链路。
- 提高资源利用率：负载均衡、无状态化、并发处理、水平扩容。

源稿中的接入层、反向代理、负载均衡、API 网关都属于这类设计的一部分。接入层负责把请求安全、稳定地引导到后端；应用层则承接鉴权、限流、监控、协议转换等横切能力，避免每个服务重复实现。

### 扩展性设计

扩展性不只是机器扩容，还包括能力扩展和团队协作扩展。一个系统具备扩展性，通常意味着：

- 新功能可以在局部加模块，而不是全局重写。
- 热点链路可以单独扩容，而不是整套系统一起放大。
- 数据模型允许分片、副本、冷热分离或分层存储。
- 接口和协议演进有兼容策略，不会轻易破坏调用方。

扩展性设计的前提，是承认不同场景要用不同模式。单体、微服务、事件驱动、服务网格都不是银弹，它们只是适配不同规模和治理能力的手段。

## 方案评审与风险管理

设计文档写完并不代表工作结束。评审和风险管理，决定了方案能否真正落地。

### 评审到底在评什么

评审不是挑文档格式，而是检查这几个层面：

- 问题定义是否准确，目标和非目标是否清晰。
- 方案是否能支撑容量、性能和稳定性目标。
- 关键取舍是否解释充分，是否讨论过替代方案。
- 风险是否被识别，是否有缓解和回滚措施。
- 任务拆分、排期和依赖是否现实。

评审记录也应成为设计的一部分。哪些问题被提出、哪些决策被修改、哪些风险被接受，后续都应该可追溯。

### 风险管理要前置

好的设计会在文档里提前暴露风险，而不是等上线时才发现。常见风险包括：

- 性能风险：容量估算过于乐观，核心依赖被低估。
- 一致性风险：异步链路过多，缺少补偿或对账机制。
- 交付风险：方案依赖过多新组件，团队缺乏经验。
- 安全风险：权限、审计、隐私保护设计滞后。
- 运维风险：监控指标不全，无法快速发现和定位故障。

风险管理不是把所有风险消灭，而是识别、排序并给出应对策略，例如灰度发布、压测验证、开关控制、回滚方案和演练计划。

## 本章小结

技术方案设计的本质，是把一个模糊问题逐步收敛为一套可评审、可实施、可演进的工程决策。本章的方法论可以压缩为一条主线：

1. 先澄清问题与边界，再谈方案。
2. 先用数据识别容量与约束，再谈组件选型。
3. 先讲核心链路、数据流和状态流，再补充横切能力。
4. 最后用评审、风险和排期把方案从“想法”变成“工程计划”。

如果第 1 章回答的是“系统设计在解决什么问题”，那么本章回答的就是“面对一个真实需求，如何有步骤地把问题做成方案”。

延伸阅读：
- `source/_posts/system-design/06-tech-design-methodology.md`
- [Clean Architecture（整洁架构）中文翻译](https://github.com/leewaiho/Clean-Architecture-zh/tree/master?tab=readme-ov-file)
- [技术方案设计的规范与模板](https://juejin.cn/post/7294443057835425855)

## 本章面试题与追问

1. 为什么技术方案不能一开始就讲实现细节？
   追问：背景、目标、非目标应该怎么区分？
2. 做一个新系统时，你会优先澄清哪些需求？
   追问：如果产品方给的信息很模糊，你会怎么推进？
3. 容量估算通常需要哪些关键数据？
   追问：没有历史数据时，你会如何做保守估算？
4. 你如何判断一个系统是否需要缓存、消息队列或分库分表？
   追问：这些组件分别解决什么瓶颈，又会引入哪些复杂度？
5. 技术方案里的核心链路为什么比全景图更重要？
   追问：请用下单或搜索场景举例说明。
6. 什么是数据流和状态流？为什么两者都要单独设计？
   追问：支付回调重复到达时，如何保证状态正确？
7. 设计接口时为什么强调幂等性？
   追问：幂等键和去重表分别适合哪些场景？
8. 你如何在高可用、高性能和扩展性之间做取舍？
   追问：如果预算有限，你会优先保证哪一项，为什么？
9. 技术评审时你最关注哪些问题？
   追问：如果评审人提出完全不同的方案，你会如何比较与回应？
10. 一个设计方案上线前，必须准备哪些风险控制手段？
   追问：灰度、压测、监控、回滚各自解决什么问题？
