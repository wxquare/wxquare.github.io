# 第 8 章 系统可靠性工程

可靠性工程的目标，不是让系统“永不出错”，而是让系统在有限预算内可观测、可恢复、可演进。真正成熟的稳定性建设，既包括监控、应急、容量和发布，也包括面向故障的架构设计、保护性机制和复盘文化。

## 可靠性的核心指标

讨论可靠性时，第一步不是喊“高可用”，而是先把目标量化。最常见的一组指标是 SLI、SLO、SLA：

| 指标 | 含义 | 典型问题 |
| --- | --- | --- |
| SLI | 服务真实表现的观测指标 | 我们怎么衡量延迟、成功率、可用性？ |
| SLO | 团队内部承诺的目标 | 系统应该做到多稳定？ |
| SLA | 面向客户的协议承诺 | 违约后要承担什么责任？ |

实践里，SLI 往往围绕延迟、错误率、流量和饱和度展开，也就是常说的“四个黄金信号”。但它不能只停留在系统层，还要覆盖业务层、应用层、基础设施层。

| 层次 | 关注点 |
| --- | --- |
| 业务层 | 下单成功率、支付成功率、库存一致性 |
| 应用层 | 接口延迟、异常率、线程池与连接池使用情况 |
| 系统层 | CPU、内存、磁盘、网络、容器资源使用 |

只有把业务指标放进可靠性体系，监控才真正贴近用户体验。否则系统图表看起来“机器都活着”，业务却已经不可用。

错误预算也是一个非常重要的管理工具。它不是鼓励犯错，而是把“稳定性目标”和“变更速度”绑定起来：预算充足时可以加快试错；预算透支时就该收紧发布、优先治理缺陷。

## 高可用设计原则

高可用设计的核心不是堆机器，而是减少单点、缩小故障域、提升恢复速度。常见原则可以总结为以下几类：

| 原则 | 目标 | 常见做法 |
| --- | --- | --- |
| 冗余 | 消除单点 | 多实例、多副本、双机房 |
| 解耦 | 降低故障传播 | MQ、异步化、服务边界拆分 |
| 隔离 | 控制爆炸半径 | 线程池隔离、集群隔离、租户隔离 |
| 无状态 | 提升弹性和恢复速度 | 状态外置到缓存、数据库、对象存储 |

冗余要讲层次。应用层多实例只能抗单机故障，数据层多副本才能抗节点损坏，跨机房部署才能应对更大范围故障。面试里如果只回答“多部署几个实例”，通常会被继续追问“数据库、缓存、消息队列怎么做”。

高可用还必须正视一致性和成本。副本越多、同步越强，可用性和复杂度都可能上升；跨城双活和异地多活能降低单地域风险，但会引入更重的链路治理、流量调度和数据冲突问题。工程上要回答清楚的是“在哪一层做到什么级别的可用性”，而不是抽象地追求全局最强。

## 超时、重试、幂等与隔离

很多级联故障不是由最初错误本身造成的，而是由错误放大机制造成的。超时、重试、幂等和隔离就是控制放大效应的第一层防线。

超时必须显式设置，不能依赖默认值。没有超时，请求就可能无限挂住；超时过长，线程和连接会被拖死；超时过短，又会把正常抖动误判为失败。超时策略通常要分层设置：

- 连接超时：控制建连等待。
- 读取超时：控制响应等待。
- 整体超时：控制请求全生命周期。

重试的价值是掩盖瞬时失败，但前提是失败具备重试价值，而且重试不会进一步压垮系统。设计重试时至少要回答三件事：

| 问题 | 设计点 |
| --- | --- |
| 什么错误可重试 | 只对瞬时失败、幂等操作重试 |
| 重试多少次 | 限制次数，配合退避和抖动 |
| 重试后副作用如何控制 | 依赖幂等键、状态机、去重记录 |

幂等是重试安全的基础。支付回调、订单创建、消息消费这些场景里，系统通常做不到“只收到一次”，但必须做到“多次执行结果一致”。常见手段包括业务唯一键、乐观锁、状态机、去重表和幂等 token。

隔离则用于防止局部故障拖垮全局。线程池隔离、连接池隔离、核心链路与非核心链路隔离、租户隔离，都是在缩小故障半径。没有隔离时，一个慢依赖就能把共享线程池耗尽，再把所有请求一起拖死。

## 限流、熔断与降级

保护性机制的目标不是“让所有请求都成功”，而是优先保证核心功能和系统整体不被压穿。

限流用于控制进入系统的请求量，避免瞬时流量把下游打满。常见算法有固定窗口、滑动窗口、漏桶和令牌桶。工程上更重要的是分层限流：

- 入口限流：挡住异常流量和攻击流量。
- 服务级限流：保护单个应用实例和依赖。
- 用户/租户级限流：避免少数热点用户占满公共资源。

熔断用于在依赖已经明显异常时快速失败，避免大量请求继续堆到故障节点。它通常围绕失败率、超时率和并发数等阈值工作，经历关闭、打开、半开三种状态。熔断不是为了“隐藏问题”，而是为了给系统争取恢复窗口。

降级则是明确告诉系统：在资源不足或依赖失败时，哪些能力可以临时舍弃，哪些主链路必须保住。

| 机制 | 解决的问题 | 常见例子 |
| --- | --- | --- |
| 限流 | 流量超过承载能力 | 热点秒杀、接口调用配额 |
| 熔断 | 下游异常导致雪崩 | 数据库超时后快速失败 |
| 降级 | 资源不够时保核心功能 | 关闭推荐、评论、画像等非核心能力 |

最常见的错误是只会背概念，却没有业务优先级。真正的降级设计必须回答“谁是核心交易链路，谁可以牺牲，牺牲后用户能接受到什么程度”。

## 监控、日志与追踪

监控是可靠性金字塔的底座。没有监控，故障只能靠用户投诉发现；没有好的告警，团队会在噪声中疲劳；没有日志和追踪，定位效率会非常低。

监控体系通常分三层建设：

| 层次 | 典型指标 | 目的 |
| --- | --- | --- |
| 业务监控 | 成功率、转化率、资损金额 | 尽早发现用户可感知异常 |
| 应用监控 | 延迟、错误率、线程池、连接池 | 判断服务健康与瓶颈 |
| 系统监控 | CPU、内存、磁盘、网络 | 判断节点和容器资源状态 |

告警策略要避免两个极端：过于迟钝，问题已经大面积扩散才响；过于敏感，团队每天被噪声淹没。好的告警要能体现严重度、通知对象和处理预案的对应关系。

日志更适合回答“发生了什么”，链路追踪更适合回答“慢在哪里、卡在哪一跳”。在分布式系统中，三者需要联动：监控负责发现问题，日志负责补充上下文，追踪负责定位跨服务链路中的异常点。

## 容量规划、压测与演练

很多线上事故本质上不是程序 bug，而是容量错判、流量模型失真、变更缺乏演练。容量规划要把业务增长、活动峰值和系统瓶颈串起来，而不是只盯着机器台数。

一个基本的容量规划流程通常包括：

1. 梳理核心链路和关键依赖。
2. 估算峰值 QPS、并发数、数据量和增长速度。
3. 找出最先触顶的资源项，例如数据库连接、线程池、网关带宽、搜索分片。
4. 预留安全余量，并准备扩容与降级方案。

压测的目的不是“跑出一个漂亮数字”，而是验证系统在真实流量模型下的瓶颈和退化方式。至少要分清三种压测：

| 类型 | 目的 |
| --- | --- |
| 基准压测 | 测单服务上限和资源曲线 |
| 链路压测 | 验证核心业务路径的端到端能力 |
| 故障演练 | 验证节点故障、依赖抖动、机房切换时的系统行为 |

演练比单纯压测更接近可靠性建设本质。它要求团队不仅知道系统“能扛多大流量”，还要知道“出故障时谁来响应、怎么摘流、怎么切换、怎么回滚”。没有演练，预案大概率只是文档。

## 本章小结

可靠性工程是一套从目标定义到工程落地的闭环：先用指标量化，再靠架构和保护机制降低故障概率，用监控与值班体系提高发现速度，用容量和演练提升面对变化的韧性。

本章最值得记住的是：

- 可靠性首先是可量化目标，而不是抽象口号。
- 高可用不是单一技术，而是冗余、解耦、隔离和无状态的组合。
- 超时、重试、幂等、限流、熔断、降级共同决定了故障会不会被放大。
- 监控、压测、演练和复盘，决定了团队能否持续把系统越做越稳。

## 本章面试题与追问

1. SLI、SLO、SLA 分别是什么？三者之间是什么关系？
追问：为什么很多团队有 SLA，却没有真正可执行的 SLO？

2. 可靠性指标为什么必须覆盖业务层，而不能只看 CPU、内存和错误日志？
追问：如果接口成功率正常，但支付成功率下降，你会如何解释这种现象？

3. 高可用设计通常包含哪些基本原则？
追问：冗余、解耦、隔离、无状态分别是在控制什么风险？

4. 为什么超时必须显式设置？
追问：如果一个下游偶发变慢，超时和重试配置不当会怎样把小故障放大成大故障？

5. 重试为什么必须和幂等一起设计？
追问：支付、消息消费、订单创建这三类场景里，幂等实现有什么差别？

6. 限流、熔断、降级分别在什么情况下使用？
追问：如果大促时库存服务压力飙升，你会优先在哪一层限流，哪些功能先降级？

7. 什么是线程池隔离和资源隔离？
追问：为什么共享线程池经常会把非核心故障放大成全站故障？

8. 监控、日志、追踪三者分别解决什么问题？
追问：如果接口 P99 延迟突然上升，你会怎样组合使用这三种手段定位问题？

9. 容量规划通常怎么做？
追问：如果大促峰值是日常的 8 倍，你会如何把业务指标换算成系统资源需求？

10. 压测和故障演练有什么不同？
追问：为什么很多团队压测成绩很好，线上出故障时仍然手忙脚乱？
